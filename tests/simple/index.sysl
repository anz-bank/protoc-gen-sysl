
Bar:
    @description =:
        |// This is a comment before Bar
    @package = "whatever"
    AnotherEndpoint(input <: whatever.Request):
        ...
        @description =:
            |// this is a comment before Bar.AnotherEndpoint

Foo:
    @description =:
        |// This is a comment before Foo
    @package = "whatever"
    ThisEndpoint(input <: whatever.Request):
        ...
        @description = ""

whatever:
    @package = "whatever"
    !type Money:
        Nanos <: int
        Units <: int
    !type Request:
        Query <: string
    !type Response:
        Query <: string

