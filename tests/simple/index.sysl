
Bar:
    @description =:
        |// This is a comment before Bar
    @package = "simple"
    AnotherEndpoint(input <: simple.Request):
        @description =:
            |// this is a comment before Bar.AnotherEndpoint
        return ok <: simple.Response

Foo:
    @description =:
        |// This is a comment before Foo
    @package = "simple"
    ThisEndpoint(input <: simple.Request):
        @description = ""
        return ok <: simple.Response

simple:
    @package = "simple"
    !type Money:
        Nanos <: int
        Units <: int
    !type Request:
        Query <: string
    !type Response:
        Query <: string

