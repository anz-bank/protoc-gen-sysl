syntax = "proto3";

package grpc.testing;

import "sysloption/sysloption.proto";

message Request {
    string query = 1;
}

message Response {
    string query = 1;
}

message Money {
    int64 units = 2;
    int32 nanos = 3;
}

service Foo{
    rpc thisEndpoint(Request) returns(Response){
        option (sysl.calls) = {call: "Bar.AnotherEndpoint" call: "ThisApp.DoesntExist"};
    };
}

service Bar{
    rpc AnotherEndpoint(Request) returns(Response);
}