syntax = "proto3";

package grpc.testing;

import "tests/otheroption/option.proto";

message Request {
    string query = 1;
}

message Response {
    string query = 1;
}

service Bar{
    rpc AnotherEndpoint(Request) returns(Response){
        option (foobar.bars) = {
            bar: "thisShouldn'tBeProcessed"
        };
    }
}